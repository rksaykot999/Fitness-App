# Fitness App

This is a React Native fitness app developed using Expo. It includes features for tracking workouts, progress, and user profiles.

## Installation

To install the app, clone the repository and run:

```bash
npm install
```

## Usage

To start the app, run:

```bash
npm start
```
